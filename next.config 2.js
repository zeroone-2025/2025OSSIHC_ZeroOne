/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
}

// next.config.js
module.exports = {
  devIndicators: {
    buildActivity: false,
  },
};

module.exports = nextConfig